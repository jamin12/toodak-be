package org.toodakbe.domain.profile.enums

/**
 * 아바타 톤 색상 팔레트. 첫 값이 가입 시 기본값.
 *
 * 프론트의 `avatarTone` 디자인 토큰과 1:1 대응 — 임의 색상 대신 고정 팔레트만 허용한다.
 */
enum class AvatarTone {
    PINK,
    AMBER,
    ROSE,
    GREEN,
    BLUE,
    GRAPE,
}
